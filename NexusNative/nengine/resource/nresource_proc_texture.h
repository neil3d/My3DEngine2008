#pragma once

/**
 *	过程纹理
 *	.参考Allegorithmic's Substance Air,根据一些参数可以从Diffuse贴图生成Normal,Speculer贴图
*/

class nresource_proc_texture
{
public:
	nresource_proc_texture(void);
	virtual ~nresource_proc_texture(void);
};
